// const express = require("express");
// const router = express.Router();

// const { joiContactSchema } = require("../../../model");
// const { validation } = require("../../../middleware");
// const ctrl = require("../../../controllers/users");

// const validationmiddleware = validation(joiContactSchema);

// router.get("/", ctrl.listContacts);

// router.get("/:contactId", ctrl.getById);

// router.post("/users/signup", validationmiddleware, ctrl.signup);
// router.post("/users/login", validationmiddleware, ctrl.login);
// router.post("/users/logout", validationmiddleware, ctrl.logout);

// router.put("/:contactId", ctrl.update);

// router.delete("/:contactId", ctrl.remove);

// module.exports = router;
